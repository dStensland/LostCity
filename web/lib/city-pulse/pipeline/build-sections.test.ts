import { describe, it } from "vitest";

describe("buildSections", () => {
  it.skip("emits planning_horizon when the horizon pool contains valid canonical targets", () => {
    // Planning Horizon section removed from feed in commit 89be688d
    // Tests for buildPlanningHorizonSection kept in section-builders.test.ts for future restoration
  });
});
