// Minimal loading state for root page - the splash page is a simple client component
// that loads quickly, so we just show nothing to avoid visual flash
export default function HomeLoading() {
  return null;
}
